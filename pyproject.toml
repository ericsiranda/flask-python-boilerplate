[project]
name = "flask-python-boilerplate"
version = "0.1.0"
description = "Flask + Vercel"
dependencies = [
    "Flask",
    "gunicorn~=23.0.0",
    "vercel",
    "vercel_blob",
    "redis",
    "bcrypt",
]

[tool.vercel]
entrypoint = "main:app"